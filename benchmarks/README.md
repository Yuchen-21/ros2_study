# Benchmarks

该目录在 Stage 06～08 存放参数矩阵、原始结果 schema 和分析脚本。基准结果不得脱离环境元数据单独提交。

最低元数据：commit、OS/kernel、CPU governor、CPU affinity、compiler/build type、ROS distro、RMW/DDS version、QoS、transport、message size/rate、运行轮数。
